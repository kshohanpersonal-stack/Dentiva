name: probe
on:
  push:
    branches: [arena/01a0c217-dentiva]
    paths: ['.github/workflows/probe.yml']
jobs:
  win:
    runs-on: windows-latest
    steps:
      - run: echo "windows runner ok"; node -v
        shell: bash
